# Copy to relay/.env and fill in. Never commit the real file.
DISCORD_TOKEN=paste-the-bot-token-here
DISCORD_CHANNEL=paste-the-channel-id-here
RELAY_PORT=8080
# Optional. When set, the controller must send the same value (host.secret in config.lua).
RELAY_SECRET=
